@model TransCanada.Models.ServiceProd

@{
    ViewBag.Title = "UpdateServiceType";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>@TempData["Product_service"].ToString()->Panel List</h2>
@{
    TempData.Keep("Product_service");
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })


        @Html.HiddenFor(model => model.Id)

        @*<div class="form-group">
                @Html.LabelFor(model => model.LabType, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.LabType, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.LabType, "", new { @class = "text-danger" })
                </div>
            </div>

            <br />*@
        @Html.ActionLink("Add Panel", "AddProductSubService", new { id = Model.Id })
        <br />
        <br />
        <table class="table" id="Table">
            <thead>
                <tr>

                    <th>
                        Panel
                    </th>
                    <th>
                        Panel Description
                    </th>
                    <th>
                        Lab Name
                    </th>
                    <th>
                        Purchase Cost With MRO
                    </th>
                    <th>
                        Billig Price
                    </th>
                    <th></th>
                    <th></th>
                </tr>

            </thead>
            <tbody>
                @foreach (var item in ViewData["Data"] as List<TransCanada.Models.ProductSubservice>)
                {
                    <tr>
                        <td>@item.lab_services_description</td>
                        <td>@item.lab_services_ext_description</td>
                        <td>@item.Lab_name</td>
                        <td>@item.service_charges</td>
                        <td>@item.Billing_Price</td>
                        <td>@Html.ActionLink("Edit", "UpdateProductSubService", new { id = item.id })</td>
                        <td>@Html.ActionLink("Delete", "DeleteProductsubservice", new { id = item.id }, new { onclick = "return confirm('Are you sure you want to delete this ?');" })</td>
                    </tr>
                }
            </tbody>
        </table>
        <div>

            @*<div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save" class="btn btn-default" />
                    </div>
                </div>*@
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "ServiceTypeList")
</div>
